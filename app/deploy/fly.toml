app = "topogram-starter-deployment-stack"
primary_region = "ord"

[build]
  dockerfile = "services/app_api/Dockerfile"

[env]
  PORT = "3000"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
